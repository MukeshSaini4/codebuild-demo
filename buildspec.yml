version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies (none)"

  build:
    commands:
      - echo "Building static website..."
      - echo "Build completed!"

artifacts:
  files:
    - '**/*'
  base-directory: .